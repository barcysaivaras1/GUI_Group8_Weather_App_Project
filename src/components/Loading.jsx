// Loading.js

import React from 'react';

const Loading = () => (
    <div className="loading-container">
        <div className="loading-spinner">
            {/* You can use a GIF or create a CSS-based spinner */}
            Loading...
        </div>
    </div>
);

export default Loading;
